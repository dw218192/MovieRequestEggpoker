{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Flask: main.py",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/app/main.py",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}